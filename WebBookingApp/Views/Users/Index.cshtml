@model IEnumerable<WebBookingApp.Models.User>

@{
    ViewData["Title"] = "Users List";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - BookingAppWeb</title>
    <style>
        /* ======== RESETARE CSS ======== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #283c86, #45a247);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        /* ======== CONTAINER CENTRAL ======== */
        .container {
            width: 80%;
            max-width: 900px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        h2 {
            margin-bottom: 20px;
            font-size: 2rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: white;
        }

        /* ======== BUTON "ADD USER" ======== */
        .btn-add {
            display: inline-block;
            padding: 12px 20px;
            background: #28a745;
            color: white;
            font-size: 1rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
            transition: all 0.3s ease-in-out;
        }

        .btn-add:hover {
            background: #218838;
            box-shadow: 0 5px 15px rgba(40, 167, 69, 0.4);
        }

        /* ======== TABEL STILIZAT ======== */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            overflow: hidden;
        }

        thead {
            background: rgba(255, 255, 255, 0.3);
        }

        th, td {
            padding: 15px;
            text-align: center;
            color: white;
        }

        th {
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        tbody tr {
            transition: background 0.2s ease-in-out;
        }

        tbody tr:hover {
            background: rgba(255, 255, 255, 0.3);
        }

        /* ======== BUTOANE EDIT & DELETE ======== */
        .btn-edit {
            background: #ffc107;
            color: black;
            padding: 8px 14px;
            border: none;
            border-radius: 5px;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.2s ease-in-out;
            text-decoration: none;
            display: inline-block;
        }

        .btn-edit:hover {
            background: #e0a800;
        }

        .btn-delete {
            background: #dc3545;
            color: white;
            padding: 8px 14px;
            border: none;
            border-radius: 5px;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.2s ease-in-out;
            text-decoration: none;
            display: inline-block;
        }

        .btn-delete:hover {
            background: #c82333;
        }
    </style>
</head>
<body>
<div class="container">
    <h2>Users</h2>

    <a asp-action="Create" class="btn-add">+ Add User</a>

    <table>
        <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.Name</td>
                <td>@user.Email</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@user.Id" class="btn-edit">Edit</a>
                    <a asp-action="Delete" asp-route-id="@user.Id" class="btn-delete">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
</body>
</html>
